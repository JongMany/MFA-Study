import clsx from "clsx";

export const wrapper = clsx(
  "network:flex",
  "network:flex-col",
  "network:bg-white",
  "network:p-4",
  "network:rounded-lg",
  "network:gap-4"
);

export const connections = clsx(
  "network:grid",
  "network:grid-cols-4",
  "network:gap-4"
);
