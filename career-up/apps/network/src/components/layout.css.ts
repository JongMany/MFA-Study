import clsx from "clsx";

export const wrapper = clsx(
  "network:flex",
  "network:flex-row",
  "network:gap-6",
  "network:mx-auto",
  "network:my-0",
  "network:mx-w-1128px",
  "network:p-4"
);

export const left = clsx(
  "network:flex",
  "network:flex-col",
  "network:w-225px",
  "network:gap-2.5"
);

export const center = clsx(
  "network:flex",
  "network:flex-col",
  "network:w-879px",
  "network:gap-2.5"
);
