import clsx from "clsx";

export const wrapper = clsx(
  "network:flex",
  "network:flex-row",
  "network:bg-white",
  "network:p-4",
  "network:rounded-lg",
  "network:justify-between",
  "network:items-center"
);
